# Contributing to Ayah

Thank you for your interest in Ayah. The project is currently in its
**design phase** — see `ARCHITECTURE.md` for the full technical design and
current stage. Until the initial app shell and Quran import pipeline exist
(the next stages of work), the most useful contributions are review and
discussion of the architecture itself, particularly around the licensing
decisions documented in `THIRD_PARTY_LICENSES.md`.

## Ground rules

- **Read `ARCHITECTURE.md` and `PRIVACY.md` first.** Ayah's priorities, in
  order, are: correctness of Quran data, privacy and security, low
  resource usage, a native macOS experience, a simple and maintainable
  architecture, and open-source friendliness. Changes that trade away a
  higher priority for a lower one need strong justification.
- **No new network dependencies.** Any change that would require the
  `com.apple.security.network.client` entitlement, or that adds a
  dependency which performs networking, analytics, or telemetry, will be
  rejected regardless of how useful the feature is. This is a hard
  architectural boundary, not a style preference.
- **Quran text is immutable and never hand-edited.** The contents of
  `Resources/Quran/` are generated only by `Scripts/import_quran` from the
  documented upstream source in `Resources/Quran/SOURCE.md`. Never edit
  `quran.sqlite`, its exported text, `VERSION`, `CHECKSUM`, or
  `MANIFEST.json` directly. Re-running the importer requires the whole
  downloaded KFGQPC archive plus the official MD5/SHA-1 values published
  on the same download page (`--source-archive`/`--official-md5`/
  `--official-sha1` — see `CLAUDE.md`'s documented command); the importer
  verifies the archive against those official values, extracts the CSV
  itself, and regenerates `VERSION`/`CHECKSUM`/`SOURCE.md`/`MANIFEST.json`
  together — `VERSION`/`CHECKSUM` are regenerated by `import_quran`
  itself, `Scripts/verify_quran` only checks them, it doesn't produce
  them. Any pull request that changes anything under `Resources/Quran/`
  **must** also update `VERSION`, `CHECKSUM`, and `MANIFEST.json` together
  — `.github/workflows/quran-integrity.yml` flags PRs that touch the data
  without the corresponding metadata bump, and reviewers should treat an
  unexplained Quran-data diff as a reason to slow down, not merge quickly.
- **Keep it simple.** Don't add abstractions, settings, or configuration
  options that don't solve a real, current problem. See the "Major
  implementation risks" and phased build order in `ARCHITECTURE.md` for
  what's actually in scope right now.

## Reporting issues

Use GitHub issues for bugs, feature discussion, and architecture questions.
For anything security-sensitive, see `SECURITY.md`.

## License

By contributing, you agree that your contributions to Ayah's source code
will be licensed under the project's MIT license (see `LICENSE`). Please do
not contribute code copied from incompatibly-licensed projects — in
particular, nothing derived from GPL-licensed notch-UI projects studied for
reference (see `THIRD_PARTY_LICENSES.md`).
