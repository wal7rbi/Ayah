/// Marker namespace confirming AyahKit links correctly into the app target.
/// Prayer and Notifications (see ARCHITECTURE.md) are still later build
/// stages, kept out on purpose; Quran, Memorization, Scheduling, Settings,
/// and Persistence are already present.
public enum AyahKit {
    public static let version = "1.0.1"
}
